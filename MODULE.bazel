"""Parson JSON library - A lightweight JSON library in C."""

module(
    name = "parson",
    version = "1.5.3",
)

bazel_dep(name = "rules_cc", version = "0.1.4")
bazel_dep(name = "platforms", version = "1.0.0")
